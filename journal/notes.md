# notes.md — справочник и черновик памяти

## Метод обучения (договорённость с пользователем)
- Не читать сразу готовое решение. Сначала прочитать только блок-комментарий
  (условие + пример), закрыть остальное, попробовать написать самому.
  Застрял — подглядеть ИДЕЮ, не код. Так паттерн закрепляется лучше.
- Повтор вчерашней задачи по памяти важнее количества новых задач.
- На Senior отдельно оценивают: проговаривание решения вслух и оценку сложности.
- При разборе каждой задачи работать со шпаргалкой `docs/heuristics.md`:
  явно называть применимые приёмы, дописывать задачу как пример к ним,
  новые общие приёмы добавлять отдельными пунктами. Примеры сквозных приёмов:
  «условие цикла = отрицание "работа закончена"» (два указателя, бинарный поиск
  low <= high, скользящее окно, фолбэк после цикла в Valid Parentheses),
  «смотри на донора» (Move Zeroes, слияние в mergesort).

## Что спрашивают на алгосекции (из веб-ресёрча 27.06.2026)
- Ядро: массивы, строки, хеш-таблицы, деревья. DP и сложные графы на скрининге редки.
- Т-Банк: бывает отборочный «Контест»; типичные задачи — корректность скобок,
  нормализация данных из API, обход деревьев. Оценивают чистоту кода и оценку сложности.
- Источники в algo-prep-plan.md (раздел «Источники»).

## Технические заметки
- Песочница Claude = одноразовый Linux x64, сброс между сессиями.
  JDK тарбол лежит в папке проекта → распаковывать в /tmp/jdk в начале сессии
  (команды в CLAUDE.md, раздел «Сборка в песочнице»). javac в системе нет, только JRE 11.
- Пути: папка проекта в песочнице = /sessions/<id>/mnt/algo-roadmap (id меняется!).

## Открытые вопросы
- Пользователь ещё не выбрал явно план A или B (склонялись к старту с A).
