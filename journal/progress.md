# progress.md — журнал сделанного (только дописывать в конец)

## 2026-06-27
- Определены вводные: Java Senior, рынок РФ/СНГ, русскоязычные собесы, референс Т-Банк.
  Главный риск — нехватка усидчивости, поэтому упор на минимум и быстрый фидбэк.
- Собран `algo-prep-plan.md`: ядро из 6 блоков паттернов, план A (1–2 нед) и план B (месяц),
  задачи с ссылками на LeetCode, опорные решения на Java. Основан на свежем веб-ресёрче
  (карьерные страницы Т-Банка, разборы собеседований, гайды 2025).
- Согласована структура кода: блок = пакет, задача = отдельный класс, тесты в main(),
  без JUnit/сборщиков. Единый шаблон класса (см. CLAUDE.md).
- Реализован БЛОК 1 «Массивы, два указателя» — 4 класса в src/block01_arrays_twopointers/:
  ReverseArray, ValidPalindrome, MergeSortedArray, TwoSumSorted.
- Настроена компиляция в песочнице: пользователь положил Linux-JDK 25 тарбол,
  распаковал в /tmp/jdk. Блок 1 скомпилирован и прогнан — 16/16 тестов PASS.
- Добавлен .gitignore (*.tar.gz, out/, target/).
- Заведён рабочий журнал (work-journal): CLAUDE.md, progress.md, todo.md, notes.md. ADR не ведём.
- Журнальные файлы перенесены в папку journal/ (progress, todo, notes). CLAUDE.md остался в корне
  как точка входа и указывает на journal/.

## 2026-07-06
- Обсудили плагины Cowork. Пользователь (Lela) установила два: Productivity
  (задачи/память/планирование, коннекторы Asana, Jira/Confluence, ClickUp, Linear,
  Monday, Notion, Slack + календарь/почта) и Engineering (ревью кода, ADR, debug,
  документация, стендапы; коннекторы GitHub, Datadog, PagerDuty). Коннекторы пока
  НЕ авторизованы (OAuth делается в настройках коннекторов claude.ai, не из чата).
  Скиллы срабатывают по смыслу запроса — явно вызывать не обязательно.
- Разобрали скилл algo-rithmic-art (p5.js, генеративное абстрактное искусство). Вывод:
  для учебных визуализаций алгоритмов НЕ подходит — он про декоративные узоры, не про
  наглядность. Не используем.
- СДЕЛАЛИ первую обучающую визуализацию: src/block01_arrays_twopointers/TwoSumSorted.viz.html
  Это интерактивная пошаговая анимация двух указателей ровно по логике TwoSumSorted.java.
  Пользователю ОЧЕНЬ понравилось — хочет такие же для остальных задач.

### Рецепт визуализаций (ВАЖНО — воспроизвести в новом чате)
  Формат: один самодостаточный HTML-файл  (имя <Класс>.viz.html),
  чистый HTML+CSS+JS, БЕЗ библиотек (p5.js не нужен), открывается в браузере без установки.
  Составляющие, которые понравились и надо сохранять как стиль:
    * массив рисуется <div>-ячейками, у каждой ячейки индекс снизу;
    * указатели (L/R или i/j) подписаны над/под ячейками, двигаются по шагам,
      отсечённая/пройденная часть тускнеет (класс dim), совпадение — зелёная подсветка (hit);
    * массив шагов steps[] строится заранее из логики алгоритма, каждый шаг = {состояние, line, msg};
    * статус-строка объясняет каждый шаг словами по-русски
      (напр. "sum = 2 + 15 = 17 > target → сдвигаем r влево");
    * справа блок кода с подсветкой активной строки (соответствие через поле line);
    * управление: кнопки Назад/Вперёд, стрелки ← →, авто-проигрыв (пробел), Сброс,
      поля ввода массива и target + кнопка "Построить" для пересборки;
    * тёмная тема, моноширинный шрифт для чисел/кода.
  Как пользователь будет просить: "сделай визуализацию как для twosum, но для <Класс>".
  Осталось сделать по этому шаблону: ValidPalindrome, MergeSortedArray (и дальше по блокам).

- Про перенос текущего чата в проект algo-roadmap: готовый чат задним числом привязать
  нельзя, привязка задаётся при старте. Договорились: новые беседы стартовать ИЗ проекта,
  а контекст сохранять в этот журнал (эта запись — как раз для того, чтобы новый чат продолжил).

## 2026-07-07
- Стиль кода блока 1 доведён под предпочтения Lela:
  * тела всех if/else/for/while — в фигурных скобках (даже однострочные);
  * говорящие имена вместо однобуквенных: leftIndex/rightIndex, writeIndex,
    firstIndex/secondIndex, nums1Count/nums2Count, temp (вместо l/r/i/j/k/m/n/tmp);
  * имена из условия LeetCode (nums, target) не трогаем.
  Два новых скилла (создаёт Lela сама через Settings → Capabilities):
    - java-code-braces-for-control-bodies — скобки у тел управляющих конструкций;
    - java-meaningful-names — раскрывающие имена переменных/параметров.
  В CLAUDE.md добавлена только ссылка на скилл про скобки (без дублирования содержимого).
- НОВЫЙ ФОРМАТ описаний задач (заменил блочные комментарии над классами):
  * полное условие каждой задачи вынесено в отдельный markdown-файл
    `docs/problems/blockNN_<тема>/<Класс>.md` — литкодовский формат с нормальной
    разметкой (условие → примеры в блоках кода → ограничения → идея → сложность
    → ссылки на визуализацию и LeetCode). Текст СВОЙ, не копия страницы LeetCode.
  * над классом теперь Javadoc /** ... */: краткое описание (2-4 строки), паттерн,
    сложность и @see со ссылкой на .md (путь ../../docs/problems/...).
  * причина: в блочном комментарии Java вся markdown-разметка теряется, читать
    неудобно; в .md — красиво рендерится.
  Сделано для всех 4 задач блока 1. Номера: MergeSortedArray=88, TwoSumSorted=167,
  ValidPalindrome=125, ReverseArray — аналог 344 (для массива чисел).
  Соглашение зафиксировано в CLAUDE.md (раздел «Соглашения по коду» + структура docs).
  ВАЖНО для новых блоков: сразу делать по этой схеме (.md + Javadoc), не блочный комментарий.
- Визуализация MergeSortedArray.html готова по рецепту (два массива, указатели
  firstIndex/secondIndex/writeIndex, слияние с конца, подсветка записи). Логика
  прогнана тестами. Осталась визуализация ValidPalindrome.

## 2026-07-10
- В отдельном чате разобрали Two Sum «в лоб» (вложенный цикл O(n²), «цикл по парам
  элементов»), затем HashMap за один проход и trade-off время↔память. Тот Клод не смог
  создать файлы — перенесли работу сюда.
- ЗАВЕДЁН БЛОК 3 «Хеш-таблицы» — первая задача:
  * src/block03_hashtables/TwoSum.java — LeetCode 1. Эталон twoSum() через HashMap
    (complement = target - nums[i], put ПОСЛЕ проверки), + baseline twoSumBruteForce()
    O(n²) как отправная точка для собеса. Javadoc + @see, тесты в main().
  * docs/problems/block03_hashtables/TwoSum.md — условие 0-based, примеры, ограничения,
    заметка про переполнение int на сумме (diapазон до 1e9), сравнение HashMap vs BruteForce.
  * docs/problems/block03_hashtables/Pattern_HashMap.md — обзор паттерна (аналог
    Pattern_TwoPointers.md): формы complement/частоты/seen-set/группировка, сигналы,
    trade-off, амортизированное O(1).
  * docs/visualizations/block03_hashtables/TwoSum.html — пошаговая анимация one-pass:
    массив + бегущий указатель i, справа растущая HashMap, подсветка complement/находки.
  * Скомпилировано JDK 25 в песочнице — 7/7 PASS.
  Осталось по блоку 3: ContainsDuplicate, GroupAnagrams, MajorityElement.
  Примечание: имя файла визуализации — <Класс>.html (как в CLAUDE.md и block01), не .viz.html.

## 2026-08-04
- Перерыв в работе с 10 июля (18 июля — только правка комментария в ReverseArray).
  Сессия начата с восстановления контекста по journal/.
- Зафиксированы два правила работы с инструментами (записаны в память Клода):
  * из desktop-commander использовать ТОЛЬКО терминал (start_process, PowerShell)
    и чтение файлов (read_file); edit_block/write_file/start_search и прочее — нельзя;
  * файлы внутри папки проекта читать и править обычными Read/Write/Edit.
- ПЕРЕПРОВЕРЕНА сверка `plan-vs-mcdowell-crosscheck.md` по тексту RAG-книги.
  Причина: Lela не смогла найти в книге то, на что ссылался файл. Нашлись три
  проблемы, все исправлены в самом crosscheck (там же — раздел «История сверки»):
  * фразы-якоря для задач 1.2 и 10.3 не находились Ctrl+F, потому что OCR склеил
    слова на переносах строк («строкаперестановкой», «циклическисдвинут»).
    Общий вывод на будущее: любые фразы-якоря по этому RAG-файлу подбирать
    короткими, не пересекающими место переноса;
  * Reverse String (344) был привязан к обзору главы 1 — на самом деле это
    отдельная задача 12.2 из главы «C и C++» (вопросы с. 165 / решения с. 443);
  * Majority Element (169) числился отсутствующим в книге — на самом деле это
    задача 17.10 «Доминирующее значение», глава «Сложные задачи»
    (вопросы с. 190 / решения с. 560), тот же алгоритм Бойера—Мура.
  Подтверждено без изменений: все номера страниц по оглавлению и номера задач
  1.1, 1.2, 1.4, 2.1, 2.6, 4.1, 4.4, 4.5, 8.1, 8.9, 8.10, 8.11, 10.1, 10.2, 10.3,
  16.17, 16.24; отсутствие в книге Valid Parentheses (есть только генерация 8.9),
  Symmetric Tree и Move Zeroes.
  Практический итог для плана: MajorityElement (блок 3) теперь можно готовить
  по книге, а не только по LeetCode.
- Запушено в master, коммит a405eb2.
- Договорённость по темпу: блок 2 «Строки + стек» делаем ПО ОДНОЙ задаче за подход,
  а не все четыре сразу.
- ЗАВЕДЁН БЛОК 2 «Строки + стек» — первая задача ValidParentheses (LeetCode 20),
  выбрана первой как любимая задача Т-Банка и как дыра, не закрытая книгой:
  * src/block02_strings_stack/ValidParentheses.java — эталон isValid() на
    ArrayDeque (в стек кладём открывающую) + вторая реализация
    isValidPushExpected() (в стек кладём ОЖИДАЕМУЮ закрывающую — сравнение одно,
    функция сопоставления типов не нужна). Плюс приватные isOpening
    и isMatchingPair. Обе реализации гоняются по одному набору случаев.
  * docs/problems/block02_strings_stack/ValidParentheses.md — условие, 5 примеров,
    разбор трёх способов получить false, заметка про вырожденный вариант с одним
    типом скобок (стек вырождается в счётчик, O(1) памяти) и про то, что книга
    эту задачу НЕ закрывает.
  * docs/visualizations/block02_strings_stack/ValidParentheses.html — по общему
    рецепту: строка ячейками, стек справа (вершина сверху), подсветка
    push/pop/совпадения/промаха, панель кода с активной строкой, пресеты
    «([{}])», «([)]», «(», «)», пустая строка.
  * Тесты в main() покрывают все ветви, включая оба return false внутри цикла
    и ОБА исхода финального return (стек пуст / не пуст). 32/32 PASS.
- Компиляция и прогон сделаны ЛОКАЛЬНО на машине Lela (JDK 25 из JetBrains
  Runtime, javac/java уже в PATH), а не в Linux-песочнице: тарбола JDK
  в проекте сейчас нет. Команда, которая работает в PowerShell:
    javac -encoding UTF-8 -d out\claudecheck src\block02_strings_stack\*.java
    [Console]::OutputEncoding=[System.Text.Encoding]::UTF8
    java "-Dfile.encoding=UTF-8" "-Dstdout.encoding=UTF-8" -cp out\claudecheck block02_strings_stack.ValidParentheses
  ВАЖНО: флаги вида -Dfile.encoding=UTF-8 в PowerShell надо брать в кавычки,
  иначе он их калечит («Could not find or load main class .encoding=UTF-8»).
- Логика визуализации отдельно сверена с Java: скрипт из HTML прогнан в Node
  по тем же 12 тест-кейсам, все ответы совпали с Java-решением.
- В docs/heuristics.md добавлен ПРИЁМ 4 «Копишь состояние — проверь, что оно
  израсходовано»: если внутри цикла есть шаг «отложить на потом» (push/+1),
  то финальный return после цикла — не голое true, а проверка накопителя
  (stack.isEmpty() / balance == 0). ValidParentheses дописан примером и к приёму 3.
- Раздел про сборку в CLAUDE.md переписан: тарбола JDK в проекте нет, собираем
  ЛОКАЛЬНО через desktop-commander (PowerShell). Песочница остаётся для не-Java
  проверок.
- НОВОЕ НАПРАВЛЕНИЕ: заведена папка `docs/datastructures/` — справочники по
  структурам данных стандартной библиотеки, отдельно от разбора задач.
  Причина: Lela после перерыва поняла, что забыла матчасть, а в docs/problems
  ей не место (там задачи, тут инструменты).
  * README.md — оглавление и соглашения папки;
  * Queue.md — интерфейс, шесть методов в двух вариантах (исключение против
    спец. значения), почему offer нужен только ограниченным очередям,
    PriorityQueue и её две ловушки (не FIFO; toString НЕ отсортирован),
    блокирующие очереди, роль очереди в BFS, типичные вопросы;
  * Deque.md — отличия от Queue, три группы методов (явные концы / унаследованные
    от Queue / стековые), почему java.util.Stack не нужен (наследует Vector,
    тянет List, итерируется от дна), ArrayDeque изнутри, запрет null, реализации,
    скользящее окно максимума как случай, где нужны оба конца, типичные вопросы;
  * img/*.svg — пять иллюстраций: FIFO-очередь, двусторонняя очередь, иерархия
    интерфейсов, кольцевой массив ArrayDeque, Deque в роли стека.
- МЕТОД РАБОТЫ для этой папки (зафиксирован в CLAUDE.md): не писать по памяти.
  * поведение проверено запуском на локальной JDK 25, фактический вывод приведён
    прямо в тексте (null-политика ArrayDeque vs LinkedList, poll/peek против
    remove/element на пустой, порядок toString у PriorityQueue, descendingIterator,
    рост массива через рефлексию);
  * внутреннее устройство сверено с исходниками из C:\Users\Ola\.jdks\jbr-25.0.3\lib\src.zip.
  * Результат: поймано устаревшее общеизвестное утверждение — «ёмкость ArrayDeque
    всегда степень двойки, индекс через & (length-1)». В JDK 25 это НЕ так:
    ёмкость произвольная (свежий массив — 17 слотов = 16 + 1 всегда пустой),
    рост вдвое до 64 и в полтора раза дальше, заворот индекса — явные inc/dec.
    Если сказать про степень двойки на собесе — формальная ошибка.
- Иллюстрации решено делать ОТДЕЛЬНЫМИ .svg файлами, а не инлайном в markdown:
  инлайновый SVG внутри .md на GitHub не рендерится.
- Добавлен третий справочник Stack.md (по отдельной просьбе Lela — стек был
  упомянут в Deque.md лишь вскользь): стек как абстрактный тип данных против
  Deque как интерфейса Java, три причины не брать java.util.Stack (наследует
  Vector → синхронизирован и является List; итерируется от дна к вершине —
  проверено: Stack.toString() = [1,2,3], ArrayDeque.toString() = [3,2,1] при
  одинаковом peek() = 3), связка «рекурсия ↔ явный стек» и что DFS отличается
  от BFS только структурой данных, монотонный стек с разбором nextGreater
  (проверено: [2,1,5,3] → [5,5,-1,-1]) и объяснением, почему это O(n).
  Иллюстрации: img/stack-lifo.svg, img/monotonic-stack.svg.
- ИСКЛЮЧЕНИЕ из правила про иллюстрации: схему иерархии интерфейсов в Deque.md
  Lela попросила заменить с SVG на ASCII-графику прямо в блоке кода.
  Файл img/hierarchy.svg за ненадобностью удалён.
  ВАЖНО про ASCII-схемы (набито шишками в этой сессии): иерархия должна остаться
  ОДНИМ связным деревом. Промежуточные варианты, которые Lela забраковала:
  (1) узел класса плюс приписка снизу стрелкой «↳ [А] AbstractQueue» — абстрактный
      класс должен быть полноценным уровнем дерева, а не сноской;
  (2) разбиение на два отдельных дерева (интерфейсы отдельно, классы отдельно) —
      «иерархию не надо было убирать», связность важнее полноты.
  Рабочий вариант: одно дерево Collection → Queue → {Deque → {ArrayDeque,
  LinkedList}, AbstractQueue → PriorityQueue}, узлы подписаны [И]/[А]/[К].
  AbstractCollection и AbstractSequentialList в дерево НЕ тащим — из-за них ветки
  разъезжаются; они остались только в блоке объявлений под схемой.
- В Deque.md дописан раздел «Что такое кольцевой массив» (по просьбе Lela):
  объяснение от проблемы (сдвиг элементов на O(n) при снятии с головы), заворот
  индекса, отличие от кольцевого СПИСКА. Плюс короткая заметка про термин:
  закреплённого перевода нет, в JDK — circular array, у Макдауэлл — «циклический
  массив», здесь — «кольцевой массив».
  ЗАМЕЧАНИЕ Lela по стилю: не приплетать в объяснения посторонние области
  («в системном программировании принято...»). Контекст — Java-собеседование,
  примеры и ссылки держать внутри него.
- БЛОК 2, вторая задача: ValidAnagram (LeetCode 242). 40/40 PASS.
  * src/block02_strings_stack/ValidAnagram.java — эталон isAnagram() на одном
    массиве int[26]: буква из s даёт +1, буква из t даёт −1, в конце массив
    обязан обнулиться. Плюс isAnagramBySorting() (O(n log n), с него стартуют
    на собесе) и isAnagramUnicode() — ответ на follow-up про не-латиницу:
    счёт по КОДОВЫМ ТОЧКАМ через codePoints(), а не по char, иначе эмодзи
    разваливается на суррогатную пару. Проверено тестами с эмодзи и кириллицей.
  * docs/problems/block02_strings_stack/ValidAnagram.md — условие, 4 примера
    (включая «набор букв тот же, количества разные»), почему алфавит из 26 букв
    даёт O(1) памяти, разбор всех трёх решений, опора в книге — задача 1.2.
  * docs/visualizations/block02_strings_stack/ValidAnagram.html — две строки,
    массив из 26 счётчиков, зелёная подсветка инкремента и розовая декремента,
    отдельная фаза сканирования массива в конце. Логика сверена с Java через
    Node по тем же 12 кейсам.
  * heuristics.md: ValidAnagram дописан примером к приёмам 3 и 4. К приёму 4 он
    даже нагляднее скобок — «накопитель израсходован» здесь буквально означает
    «все счётчики равны нулю».
- ЭКСПЕРИМЕНТ по просьбе Lela: в Deque.md почти все иллюстрации переведены с SVG
  на ASCII прямо в блоках кода. Остался единственный SVG — img/arraydeque-ring.svg
  (кольцевой массив): там важно показать «разорванное» расположение элементов
  и заворот через край, ASCII это передаёт хуже. Файлы img/deque-both-ends.svg
  и img/deque-as-stack.svg больше не используются из Deque.md, но deque-as-stack
  ещё нужен в Stack.md — НЕ удалять.
  Если эксперимент понравится — распространить на Queue.md и Stack.md и поправить
  соглашение про иллюстрации в CLAUDE.md.

## 2026-08-09 — ревизия подвидов паттерна «два указателя», пятая задача блока 1

- ПОВОД: Lela спросила, относится ли MergeSortedArray к подвиду «однонаправленное
  движение (чтение/запись, fast/slow)». Оказалось, что подвид в
  Pattern_TwoPointers.md был склеен из трёх разных вещей, а из блока была
  представлена только одна из них — и та гибридная.
- Pattern_TwoPointers.md: подвид «однонаправленное движение» разбит на три формы —
  (1) чтение и запись по одному массиву (MoveZeroes), (2) слияние двух
  последовательностей (MergeSortedArray), (3) быстрый/медленный указатель по одной
  последовательности (класса нет, это связные списки). Добавлена сводная таблица
  форм, список сигналов перенумерован (слияние выделено в отдельный сигнал 4),
  в «Частую путаницу» дописано: одинаковая скорость + разные роли — это НЕ fast/slow.
- НОВАЯ ЗАДАЧА, пятая в блоке 1: MoveZeroes (LeetCode 283). 9/9 PASS.
  * src/block01_arrays_twopointers/MoveZeroes.java — два прохода: первый сдвигает
    ненулевые влево через пару readIndex/writeIndex, второй досыпает нули в хвост.
  * docs/problems/block01_arrays_twopointers/MoveZeroes.md — условие, 3 примера,
    инвариант writeIndex <= readIndex, вариант в один проход через swap,
    отдельный раздел «Отличие от MergeSortedArray».
  * Тесты покрывают обе ветви if, оба исхода второго цикла (нулей нет — не
    начинается; все нули — делает всю работу), пустой массив, один элемент
    в двух вариантах, сохранение относительного порядка, отрицательные числа.
- heuristics.md: MoveZeroes дописан примером к приёму 2 (донор — читающий
  указатель, условие цикла привязано к нему). Добавлен ПРИЁМ 5 «Правишь массив
  на месте — следи, чтобы писатель не обгонял читателя»: инвариант
  writeIndex <= readIndex как критерий корректности in-place решения, и разворот
  направления обхода как штатное лекарство, когда инвариант ломается
  (именно поэтому MergeSortedArray пишет с конца).
- docs/visualizations/block01_arrays_twopointers/MoveZeroes.html — по общему
  рецепту: один массив, readIndex подписан сверху, writeIndex снизу, зелёная
  подсветка переноса и розовая — нулей, досыпанных во втором проходе, панель кода
  с активной строкой, шесть пресетов (обычный, нулей нет, все нули, сохранение
  порядка, ноль в конце, один элемент). Отдельная строка показывает «долг» —
  сколько нулей пропущено и сколько придётся досыпать.
- Логика визуализации сверена с Java через Node на 9 кейсах — совпала.
- ПОЙМАНА НЕТОЧНОСТЬ формулировки инварианта (нашлась именно на сверке): писать
  «writeIndex <= readIndex всегда» неверно — сразу после writeIndex++ пишущий
  индекс на мгновение равен readIndex + 1, пока не увеличился сам readIndex.
  Правильная формулировка: инвариант держится В МОМЕНТ ЗАПИСИ; устойчивый
  вариант через счётчики — «записано не больше, чем просмотрено, а разница
  равна числу пропущенных нулей». Исправлено в трёх местах: MoveZeroes.md,
  комментарий в MoveZeroes.java, приём 5 в heuristics.md; в визуализации
  «долг» считается как (readIndex + 1) − writeIndex, поэтому не уходит в минус.

## 2026-08-13 — закрыт ReverseString, четвёртая задача блока 2

- ОТКАТ лишней работы по ReverseString. Ранее по просьбе Lela был реализован
  «follow-up» reverseStringUnicode — разворот, чинящий суррогатные пары вторым
  проходом. Затем выяснилось, что у LeetCode 344 раздела Follow up нет вовсе:
  требование «на месте, O(1) памяти» стоит прямо в теле условия. Разобрались,
  что Unicode-вариант для алгосекции не нужен, и код был удалён целиком —
  метод, восемь его тестов и разбор в .md. Осталась короткая констатация,
  что follow-up у задачи нет.
  УРОК: когда выясняется, что материал не нужен, его надо удалять, а не
  переименовывать раздел. Первым заходом я именно переклеила ярлык — ошибка.
- ReverseString переведён в [x]: 10/10 PASS, Lela закончила разбирать.
- НОВАЯ ЗАДАЧА, четвёртая и последняя в блоке 2: FirstUniqueChar (LeetCode 387).
  22/22 PASS.
  * src/block02_strings_stack/FirstUniqueChar.java — эталон на int[26] в два
    прохода плюс обобщение firstUniqCharLinkedHashMap для произвольного алфавита
    (LinkedHashMap хранит порядок вставки, поэтому ответ ищется проходом по карте;
    в значении лежит record CountAndFirstIndex — счётчик и индекс первого
    появления).
  * docs/problems/block02_strings_stack/FirstUniqueChar.md — условие, 4 примера,
    раздел «Почему два прохода, а не один», таблица трёх решений со сложностью.
    Опоры в книге нет: ближайшее — задача 1.1 «Все символы уникальны?», но там
    ответ булев и порядок не важен.
  * Тесты покрывают обе ветви второго цикла, фолбэк return -1 после цикла,
    пустую строку, ответ на первой и на последней позиции, длинный вход 50 001.
- ПОЙМАНА ОШИБКА В ТЕСТЕ (не в решении): для "abcabd" я записала ожидание 4,
  хотя правильный ответ 2 — c на позиции 2 уникальна. Прогон это показал сразу.
- heuristics.md: FirstUniqueChar дописан примером к приёму 3 («существует» —
  находка внутри цикла, -1 после). Добавлен ПРИЁМ 6 «Ответ зависит от того,
  что впереди — нужен предварительный проход»: если решение про элемент i
  зависит от элементов правее, одним циклом не обойтись; и отдельно — по чему
  идёт второй цикл, потому что сводка перетасовывает порядок (массив счётчиков
  хранит буквы по алфавиту, а не по позиции в строке).
- docs/visualizations/block02_strings_stack/FirstUniqueChar.html — строка с
  подписанными индексами под каждой ячейкой, сетка 26 счётчиков, панель кода
  с активной строкой, индикатор текущего прохода (первый/второй) и семь
  пресетов. Ключевой пресет — ccdaab: на нём видно, что обход по массиву
  счётчиков вернул бы b (индекс 5) вместо d (индекс 2).
  Логика визуализации сверена с Java через Node на 11 кейсах — совпала.
- CLAUDE.md: закреплено правило делать визуализацию СРАЗУ, не спрашивая
  разрешения. Повод — я спросила «делать ли визуализацию», хотя порядок работы
  в todo.md уже включал её в стандартный цикл задачи.
- НЕСТЫКОВКА, найдена Lela: firstUniqCharLinkedHashMap был объявлен «обобщением
  на произвольный алфавит», но ключ карты стоял Character — а он вмещает 16 бит,
  потолок U+FFFF, то есть никакого произвольного алфавита. Переделано на ключ
  Integer и обход по кодовым точкам с шагом Character.charCount, как в
  ValidAnagram. Возвращаемый индекс остался в char-единицах (требование LeetCode).
  Добавлены пять Unicode-тестов, ключевой — "🐷🐽": у них ОБЩАЯ старшая половина
  суррогатной пары U+D83D, поэтому карта с ключом Character сочла бы символ
  повторяющимся и вернула бы 1 (половинку пары) вместо 0. Итого 27/27 PASS.
- CLAUDE.md: новый раздел «Утверждение шире ограничений задачи — либо тест
  на него, либо сузить текст». Повод — Lela спросила, почему тестов на эмодзи
  не было сразу. Ответ: тесты я пишу по разделу «Ограничения», а фраза
  «обобщение на произвольный алфавит» выходит за них, и её никто не проверял.
  Все три сегодняшних промаха одного вида: заявление шире проверенного.
- docs/datastructures/String.md: новый раздел «java.lang.Character не годится
  для хранения четырёхбайтных символов» — по прямой просьбе Lela сказать это
  явно, а не оставлять выводимым из «char это 16 бит». Внутри: Character.SIZE
  /BYTES/MAX_VALUE запуском, ошибка компиляции на литерале 'ПЯТАЧОК', таблица
  общей старшей половины у 🐷 и 🐽, вывод про Map<Character,…> как молчаливо
  неверный ключ, и правило «сигнатура параметра char или int показывает, умеет
  ли метод работать со всем Unicode». Плюс вопрос в раздел «Типичные вопросы
  на собеседовании».
- По просьбе Lela две реализации разнесены по разным классам: в FirstUniqueChar
  остался эталон на int[26] (12/12 PASS), вариант с картой выделен в
  FirstUniqueCharLinkedHashMap (27/27 PASS, включая Unicode и сверку ответов
  с эталоном на латинице). Метод в новом классе называется так же —
  firstUniqChar, различают их имена классов.
  ПОМЕТКА на будущее: до этого в проекте несколько реализаций одной задачи
  жили в одном классе (ValidParentheses, ValidAnagram, ReverseString).
  Новых задач это пока не касается — старые классы не трогали.
- НОВАЯ ЗАДАЧА, вторая в блоке 3: ContainsDuplicate (LeetCode 217). 27/27 PASS.
  * src/block03_hashtables/ContainsDuplicate.java — HashSet как «множество
    виденного». Ключевая деталь Java: add возвращает false, если элемент уже
    был, поэтому пара contains + add не нужна.
  * docs/problems/block03_hashtables/ContainsDuplicate.md — условие, 4 примера,
    почему массив-счётчик здесь не годится (диапазон ±10^9), таблица трат.
  * Тесты покрывают обе ветви, фолбэк после цикла, пустой массив, один элемент,
    дубликат в начале и в конце, отрицательные числа, края диапазона int
    и ранний выход на входе из 100 000 элементов.
  * docs/visualizations/block03_hashtables/ContainsDuplicate.html — массив
    с индексами, множество в виде чипов, панель кода, семь пресетов. На пресете
    1,1,2,3 видно, что цикл обрывается на индексе 1. Логика сверена с Java
    через Node на 12 кейсах — совпала.
  * Pattern_HashMap.md: сигнал 4 теперь указывает на класс, в форму «множество
    виденного» дописано про возвращаемое значение add.
  * heuristics.md: ContainsDuplicate дописан примером к приёму 3, с пометкой
    про соблазн поставить return false внутрь цикла.
  * УДАЛЕНО по требованию Lela: вариант containsDuplicateBySorting. Мотив —
    лишний объём: HashSet и есть ответ на этой задаче, требований по памяти
    в условии 217 нет. Осталось 14/14 PASS вместо 27/27.
    ПОМЕТКА: альтернативные решения добавлять, только если они закрывают
    требование ИЗ УСЛОВИЯ, а не гипотетический вопрос интервьюера.
  * Раздел про stream().distinct() сначала тоже удалили, потом Lela попросила
    вернуть — и он переписан на фактах, проверенных запуском:
    (1) досрочного выхода нет, count() протягивает поток целиком — на массиве
        из миллиона элементов с дубликатом на индексе 1 просмотрено 1 000 000
        элементов против 2 у цикла с HashSet;
    (2) IntStream.distinct() в JDK 25 реализован как boxed().distinct()
        .mapToInt(i -> i), с комментарием самих авторов про неэффективность —
        то есть добавляется ещё и упаковка в Integer;
    (3) короткозамкнутая версия через anyMatch(value -> !seen.add(value))
        действительно обрывается на 2 элементах.
- НОВАЯ ЗАДАЧА, третья в блоке 3 и ПЕРВАЯ MEDIUM в проекте: GroupAnagrams
  (LeetCode 49). 25/25 PASS.
  * src/block03_hashtables/GroupAnagrams.java — две реализации, отличающиеся
    только способом построить ключ: groupAnagrams сортирует буквы слова,
    groupAnagramsByCounts склеивает вектор частот через разделитель '#'.
    Обе раскладывают слова по корзинам через computeIfAbsent.
  * docs/problems/block03_hashtables/GroupAnagrams.md — условие, 4 примера,
    оба способа ключа с разбором, таблица сравнения, разбор computeIfAbsent.
    Отдельно проговорено, чем Medium отличается от Easy в этой задаче: идея
    та же хеш-таблица, но ключ надо СКОНСТРУИРОВАТЬ, а не взять из условия.
  * Тесты: пример из условия, пустая строка как валидное слово, две пустые
    строки в одной группе, одинаковые слова, разная длина, «те же буквы —
    разный счёт» (aacc/ccac), пустой массив, отдельные проверки buildFrequencyKey
    и подсчёт, что ни одно слово не потерялось. Порядок групп и порядок слов
    внутри группы по условию не важен, поэтому сравнение идёт через
    нормализацию обеих сторон.
  * ОТДЕЛЬНЫЙ ТЕСТ на склейку ключа: слова из 1×a+11×b и 11×a+1×b обязаны
    попасть в разные группы. Без разделителя '#' оба дали бы ключ 111...
    На обычных тестах вида eat/tea эта ошибка не всплывает — счётчик должен
    перевалить за десять.
  * docs/visualizations/block03_hashtables/GroupAnagrams.html — переключатель
    способа построения ключа, входной массив, строка «слово → ключ», корзины
    карты с подсветкой новой и дополняемой, панель кода меняется вместе
    со способом. Логика сверена с Java через Node: 11 кейсов × 2 способа.
  * heuristics.md: добавлен ПРИЁМ 7 «Придумал ключ-отпечаток — проверь его
    на склейку». Два условия к отпечатку (у эквивалентных совпадает,
    у неэквивалентных различается), и то, что забывают именно второе.
  * Pattern_HashMap.md: сигнал 5 теперь указывает на класс, в форму
    «группировка по ключу» дописано про computeIfAbsent и про то, что ключ
    здесь конструируется, а не дан.
- ЧИСТКА ВИЗУАЛИЗАЦИЙ по требованию Lela: из .html убраны текстовые блоки
  «На что смотреть» — они дублировали разбор из docs/problems. Удалено
  из шести файлов: GroupAnagrams, ContainsDuplicate, FirstUniqueChar,
  ValidAnagram, ValidParentheses, ReverseString (вместе с CSS блока).
  ОСТАВЛЕН блок в TwoSum.html: он не дублирует .md, а служит легендой самой
  схемы — что такое table, бакет и узел на картинке. Внутреннего устройства
  HashMap в TwoSum.md нет вовсе.
  Заодно выяснилось, что все пять визуализаций блока 1 таких блоков и не имели,
  то есть чистка привела набор к единому виду.
  Правило записано в CLAUDE.md: в .html допустим только текст, объясняющий
  САМУ КАРТИНКУ, разбор задачи живёт в .md.
- buildCountKey переименован в buildFrequencyKey (по просьбе Lela) — совпадает
  с термином «вектор частот» из .md и Javadoc.
- НОВЫЙ СПРАВОЧНИК docs/java/ComparableAndComparator.md. Повод — вопрос Lela
  про строку copy.sort(Comparator.comparing(...)) в normalize: тема Comparable
  и Comparator всплывает на собеседовании сама по себе, поэтому вынесена
  из комментария в код в отдельный файл.
  Содержание: таблица различий двух интерфейсов, естественный порядок String
  (заглавные раньше строчных, 'A'=65 против 'a'=97), ошибка компиляции
  на groups.sort(Comparator.naturalOrder()) для List<List<String>> —
  «inference variable T has incompatible bounds», разбор comparing как
  сравнения по ключу, замер вызовов извлекателя (4 вызова на 3 элементах,
  то есть O(n log n), а не по разу на элемент), thenComparing и reversed
  на записи Person, рассогласование с equals на TreeSet с
  CASE_INSENSITIVE_ORDER, раздел «Типичные вопросы на собеседовании».
  Всё проверено запуском на локальной JDK 25.
  В GroupAnagrams.md добавлен раздел «Как сравнить результат с ожиданием» —
  короткий разбор normalize со ссылкой на справочник.
- ПОЙМАН НЕУДАЧНЫЙ ПРИМЕР (Lela спросила «а на каких значениях это возможно»):
  в вопросе «почему нельзя писать return first.age - second.age» правило верное,
  но иллюстрация не работает — возраст неотрицательный, разность гарантированно
  влезает в int, и переполнения не будет НИКОГДА. Переполнение возможно только
  при РАЗНЫХ знаках операндов и разности от 2^31. Пример в .md заменён на
  hashCode: он бывает большим и по обе стороны нуля.
  Заведён класс src/demo/IntSubtractionOverflow.java — таблица
  из девяти пар с фактическими числами: четыре с разными знаками (знак всегда
  неверен) и пять с одинаковыми (знак всегда верен, даже на краях диапазона).
  Показательная строка: 2147483647 - (-2147483647) = -2, то есть максимум int
  «меньше» минимума на два. Проверки построены так, что тест PASS означает
  «наивный способ соврал» в первом блоке и «не соврал» во втором.
  Сами таблицы в .md не вставляем — по требованию Lela они живут только
  в java-классе, в .md стоит ссылка на него.
- НОВЫЙ ПАКЕТ src/demo/ для классов, которые не являются задачей из блока.
  Раньше такие классы клали в пакет ближайшего по теме блока, и это путало
  список задач. Перенесены оба существующих: CodePointOffsets (был в block02,
  иллюстрирует String.md) и IntSubtractionOverflow (был в block03, иллюстрирует
  ComparableAndComparator.md). Ссылки в String.md и ComparableAndComparator.md
  обновлены, правило записано в CLAUDE.md. Прогон после переноса:
  CodePointOffsets 8/8 PASS, IntSubtractionOverflow 14/14 PASS.
- НОВЫЙ СПРАВОЧНИК docs/java/Int.md. Раздел «Диапазон int и почему вычитание
  ломается» вынесен туда из ComparableAndComparator.md — тема про сам тип,
  а не про компараторы. В ComparableAndComparator.md осталась ссылка.
  Содержание Int.md, всё проверено запуском: диапазон и размер; побитовое
  представление MAX_VALUE, нуля и MIN_VALUE с шестнадцатеричным видом;
  дополнительный код и почему минус единица это все единицы; кольцо диапазона
  (MAX+1 и MIN-1) с показом сложения в битах; молчаливое переполнение
  (100000*100000 = 1410065408); Math.abs(MIN_VALUE) = MIN_VALUE;
  MIN_VALUE / -1; Math.addExact и multiplyExact с фактическим текстом
  ArithmeticException; счёт в long и почему приведение должно стоять ДО
  операции; (left+right)/2 в бинарном поиске; -7 % 3 = -1 против
  Math.floorMod(-7, 3) = 2; раздел «Типичные вопросы на собеседовании».
- НОВЫЙ СПРАВОЧНИК docs/java/Rounding.md. Вырос из вопросов Lela про перевод
  floor/ceil/round. Всё проверено запуском: сводная таблица floor, ceil, round,
  rint и (int) на восьми значениях от 2.7 до -2.7; почему усечение — это НЕ
  округление вниз (совпадают только справа от нуля); то же в целочисленном
  делении, -7/2 = -3 против floorDiv = -4; правило round как floor(x + 0.5)
  и отсюда round(-2.5) = -2; rint как банковское округление к чётному;
  таблица типов возврата (round даёт long, остальные double); края —
  (int) из double НАСЫЩАЕТСЯ границей ((int) 1e20 = MAX_VALUE, (int) NaN = 0),
  а (int) из long ОБРУБАЕТ биты ((int) 4294967296L = 0); BigDecimal с шестью
  режимами на 2.345 и оговорка про строковый конструктор.
  Перекрёстные ссылки с Int.md проставлены в обе стороны.
- НЕПОНЯТНАЯ ФОРМУЛИРОВКА, поймала Lela: в таблице RoundingMode колонка «что
  делает» у HALF_UP гласила «половина от нуля» — читается как «половина нуля».
  Раздел про RoundingMode переписан целиком и расширен:
  (1) UP и DOWN — направление относительно НУЛЯ, а не оси; вверх и вниз это
      CEILING и FLOOR. Таблица расхождения на -2.4: UP даёт -3, CEILING даёт -2;
  (2) сводная таблица всех шести режимов с результатами на 2.5 и -2.5, плюс
      деление на «безусловные» четыре и три с префиксом HALF_;
  (3) седьмой режим UNNECESSARY с фактическим текстом ArithmeticException
      «Rounding necessary»;
  (4) ВАЖНОЕ: RoundingMode.HALF_UP и Math.round — РАЗНЫЕ правила. Math.round
      кладёт половину вверх по оси, HALF_UP — прочь от нуля. На -2.5 это -2
      против -3. «Школьное» округление — это HALF_UP, а не Math.round;
  (5) строковый конструктор BigDecimal с фактическим выводом
      new BigDecimal(0.1) = 0.1000000000000000055511151231257827021181583404541015625.
  Два вопроса добавлены в раздел «Типичные вопросы».
- ЕЩЁ ОДНА ФОРМУЛИРОВКА, поймала Lela: «вверх по оси» двусмысленно — числовую
  прямую рисуют горизонтально, бо́льшие значения справа, а «вверх» намекает
  на вертикаль. Путаница идёт от самих названий floor/ceiling: метафора
  комнаты подразумевает вертикальную ось. По всему Rounding.md «вверх/вниз
  по оси» заменено на «к плюс/минус бесконечности» — формулировку, не
  зависящую от того, как нарисована ось. Заодно переименован раздел
  «Усечение — это не округление вниз» в «Усечение — это не то же самое,
  что floor».
- НОВАЯ ЗАДАЧА, четвёртая и последняя в блоке 3: MajorityElement (LeetCode 169).
  28/28 PASS. БЛОК 3 НАПИСАН ЦЕЛИКОМ.
  * src/block03_hashtables/MajorityElement.java — две реализации, и обе
    оправданы условием: majorityElement (счётчик частот через merge) как приём
    блока, majorityElementByVoting (голосование Бойера—Мура) как ответ
    на follow-up ИЗ УСЛОВИЯ — линейное время и O(1) дополнительной памяти.
  * docs/problems/block03_hashtables/MajorityElement.md — условие, 3 примера,
    оба способа, разбор «почему голосование работает» через модель взаимного
    выбывания, таблица сравнения. Отдельно проговорено, что «строго больше
    половины» — не то же самое, что «чаще всех», и что на этом держится
    весь алгоритм.
  * Тесты покрывают обе ветви обоих методов, смену кандидата, отрицательные
    числа, ноль как ответ, края диапазона int, вход на 50 001 элемент
    с перевесом ровно в один экземпляр (худший случай) и тот же вход
    с большинством вперемешку. Отдельный тест на то, что промежуточный
    кандидат ответом быть не обязан: [5,9,9,9,9].
  * docs/visualizations/block03_hashtables/MajorityElement.html — массив
    с индексами, две карточки состояния (кандидат и баланс), баланс показан
    ещё и полосками-сторонниками, подсветка «за» зелёным, «против» розовым,
    смена кандидата жёлтым. Логика сверена с Java через Node на 15 кейсах.
  * Pattern_HashMap.md: сигнал 3 теперь указывает на класс, в форму «счётчик
    частот» дописано про merge, в раздел про trade-off — абзац о том, что
    таблицу иногда удаётся убрать вовсе, но только опираясь на дополнительное
    свойство данных из условия.
- НОВЫЙ СПРАВОЧНИК docs/java/Double.md. Повод — вопрос Lela «почему 0.1
  в double не представима точно». Файл открывается двумя фразами-заготовками
  для собеседования, дальше разбор. Всё проверено запуском: 52 бита мантиссы
  и повторяющийся хвост 0011 в битах 0.1; точные значения 0.1, 0.2 и 0.3 через
  BigDecimal (первые два округлились ВВЕРХ, третье ВНИЗ — отсюда и
  0.1 + 0.2 = 0.30000000000000004); почему println(0.1) печатает «0.1»
  и прячет проблему; сравнение через эпсилон и вырождение точности
  (1e16 + 1 == 1e16); Infinity и NaN вместо исключения при делении на ноль;
  NaN != NaN для примитива, но equals у обёртки даёт true и NaN нормально
  живёт в HashSet; минус ноль (== равен нулю, а compare и equals — нет,
  1.0 / -0.0 = -Infinity); Double.MIN_VALUE как наименьшее ПОЛОЖИТЕЛЬНОЕ,
  в отличие от целочисленных типов; деньги в BigDecimal либо целым числом
  минимальных единиц. Перекрёстные ссылки с Int.md и Rounding.md.
- НОВЫЙ КЛАСС src/block03_hashtables/MajorityElementBookVersion.java — решение
  задачи 17.10 в том виде, как оно дано у Макдауэлл (по просьбе Lela; своё
  решение оставлено на месте). 27/27 PASS.
  Главное отличие от LeetCode 169: в книге НЕ гарантируется существование
  доминирующего значения, при его отсутствии надо вернуть -1. Поэтому решение
  двухпроходное: getCandidate выбирает единственного возможного претендента,
  validate пересчитывает его вхождения и подтверждает или отвергает. Второй
  проход обязателен, потому что getCandidate возвращает значение ВСЕГДА.
  Приведено и наивное решение O(n^2), с которого книга начинает рассуждение.
  Отдельным тестом зафиксирована разница: на входе [1,2,3] книжная версия
  честно даёт -1, а версия для LeetCode возвращает мусор — ей это позволено
  гарантией из условия.
- РАСХОЖДЕНИЕ В РАЗБОРНОМ МАССИВЕ задачи 17.10 (Lela спросила, нормально ли
  распознан RAG). Массив приведён в книге дважды, и версии не совпадают:
  в строку «3 1 7 1 3 7 3 7 1 7 7» (семёрок 5 из 11, доминирующего нет),
  таблицей со строкой индексов «3 1 7 1 1 7 7 3 7 7 7» (семёрок 6 из 11,
  доминирующее есть). Правильная — табличная, это доказывается трассировкой.
  Обнаружилось из-за упавшего теста: я взяла первую версию и ждала ответ 7.
  СНАЧАЛА Я СПИСАЛА ЭТО НА OCR — и ошиблась. Сверка с оригиналом PDF показала,
  что обе версии есть и в самой книге, то есть это её опечатка, а не дефект
  нашего файла. Вывод поправлен в crosscheck, в .md и в комментарии класса.
  Предупреждение записано в plan-vs-mcdowell-crosscheck.md с общим выводом —
  числовые примеры из RAG-файла перед использованием пересчитывать.
  Туда же записан путь к оригиналу книги в PDF на машине Lela (лежит вне
  примонтированной папки, читается через desktop-commander) — сверяться с ним,
  когда RAG вызывает сомнения.
  РЕЦЕПТ СВЕРКИ С PDF, работает: на машине есть pdftotext из состава Git for
  Windows (C:\Program Files\Git\mingw64\bin\pdftotext.exe) и Python 3.14.
  Извлечь текст `pdftotext -enc UTF-8 -eol unix <pdf> <txt>`, затем питоном
  разбить по \f на страницы и искать. Нумерация страниц в PDF совпадает
  с печатной, смещения нет. Всего 735 страниц.
  Точные страницы задачи 17.10: условие 191, решение 586–589.
- ЗАМЕЧАНИЕ LELA ПО СТИЛЮ, важное: я назвала разницу между своим решением
  и книжным «косметикой» — имена переменных и тернарник против if/else.
  Это неверно. Понимание кода человеком на девяносто процентов держится
  на именах, а развёрнутые ветки читаются лучше, чем компактный тернарник,
  пусть и многословнее. Для ассистента это меньше символов, для человека —
  хуже.
  В итоге тело majorityElementByVoting заменено на книжное ОДИН В ОДИН:
  имена majority и count вместо candidate и balance, развёрнутый if/else
  вместо тернарника, инициализация majority = 0 вместо nums[0].
  Сначала я оставила элемент цикла как value вместо книжного n, сославшись
  на скилл java-meaningful-names. Lela решила иначе: «книга важнее правила».
  Вернула n и x во всех местах, где код воспроизводит книгу. Исключение
  записано в CLAUDE.md: книжный код переносится один в один, а в комментарии
  над методом обязательно указывается, что он взят из книги без изменений.
- ИМЕНА ПЕРЕМЕННЫХ В majorityElementByVoting ПЕРЕСМОТРЕНЫ, разбор Lela:
  * majority — прилагательное без существительного, а переменная хранит
    именно кандидата на роль мажоритарного элемента -> majorityCandidate;
  * count звучит как «просто считаем в цикле», а величина и растёт, и падает,
    то есть это баланс -> перебрали варианты (voteBalance, supportBalance,
    unmatchedVotes, candidateBalance), сначала выбрали unmatchedVotes, потом
    Lela спросила про более дословный перевод слова «непогашенный» и по итогу
    разбора остановились на survivingVotes;
  * n -> vote, предложение Lela: перебираются голоса, метафора сходится.
  Из-за переименования метод перестал быть дословной копией книги, поэтому
  правило «книга важнее» к нему больше не применяется: n вернулся бы только
  в дословную копию. Разделение получилось чистое — MajorityElementBookVersion
  хранит книжный код один в один для сверки, MajorityElement решает ту же
  задачу читаемыми именами. В комментарии и в .md стоит таблица соответствия
  книжных и наших имён с обоснованием каждого.
  Синхронно обновлены .md и визуализация. 30/30 и 27/27 PASS, логика
  визуализации пересверена через Node на 16 кейсах.
- НОВЫЙ РАЗДЕЛ в MajorityElement.md: «Почему survivingVotes, а не другое
  английское слово». Просьба Lela — она не носитель языка, и такие тонкости
  перевода для неё ценны сами по себе. Таблица пяти вариантов с пометкой,
  какой из них стандартен в алгоритмических текстах (unmatched: unmatched
  parentheses, unmatched elements), и отдельно про ЛОЖНОГО ДРУГА: самый
  дословный перевод «непогашенный» — outstanding (outstanding loan/debt),
  но outstanding votes по-английски значит «ещё не подсчитанные голоса»,
  то есть в задаче про выборы это имя вводило бы в заблуждение.
  Общий вывод записан там же: при переводе имени переменной проверять не
  только словарное соответствие, но и ассоциации слова в предметной области.
- ПЕРЕСБОРКА ВТОРОГО КЛАССА. Lela убрала из MajorityElementBookVersion
  дублирующий getCandidate и стала вызывать MajorityElement, а потом
  сформулировала, чего хочет на самом деле: не копию книги, а решение
  и тесты для случая, КОГДА СУЩЕСТВОВАНИЕ МАЖОРИТАРНОГО ЭЛЕМЕНТА
  НЕ ГАРАНТИРУЕТСЯ.
  MajorityElementBookVersion удалён, вместо него MajorityElementNotGuaranteed
  (имя выбрала Lela из трёх вариантов). Внутри только то, ради чего класс
  существует: findMajorityElement = голосование из MajorityElement плюс
  проверка isMajority, возврат -1 при отсутствии большинства.
  Наивное O(n^2) из книги удалено — тоже решение Lela: требования из условия
  оно не закрывает.
  Тесты переписаны под задачу класса: границы порога (три вхождения из семи
  мало, четыре достаточно), ровно половина, два значения поровну, пустой
  массив, длинные входы с большинством и без, и прямое сравнение поведения
  двух классов на [1,2,3]. 20/20 PASS.
  Правило в CLAUDE.md уточнено: дословный перенос из книги — не цель сама
  по себе, он нужен только пока код действительно дословный.

## 2026-08-18 — начат блок 4 «Скользящее окно»

- ПОРЯДОК ЗАДАЧ ВЗЯТ НЕ ПО ПЛАНУ. В algo-prep-plan.md первой стоит задача 3
  (Medium), но по логике обучения удобнее начать с 643: окно фиксированной
  ширины — простейшая форма приёма, а в задаче 3 сразу две новые вещи,
  переменная ширина и состояние в виде множества. Lela согласилась.
- Pattern_SlidingWindow.md — обзорный файл блока. Суть приёма (соседние окна
  отличаются двумя элементами, значит характеристику правим на разницу),
  два подвида (фиксированная и переменная ширина) с формой кода для каждого,
  отдельный раздел «почему обе границы вместе дают O(n)» — про амортизационный
  анализ и ответ на вопрос «внутри for стоит while, это же квадрат?».
  Отдельно разобрано отличие от двух указателей: там две независимые позиции,
  здесь границы ОДНОГО объекта плюс его состояние.
- НОВАЯ ЗАДАЧА: MaxAverageSubarray (LeetCode 643). 18/18 PASS.
  * src/block04_sliding_window/MaxAverageSubarray.java — набор первого окна,
    затем сдвиг одной строкой windowSum += nums[i] - nums[i - k].
    Охранные проверки на null и на k вне диапазона стоят в начале метода
    по свежему правилу из CLAUDE.md.
  * docs/problems — условие, 4 примера, схема «кто вошёл и кто вышел»
    с индексами, ловушка целочисленного деления (без приведения к double
    ответ 12.0 вместо 12.75), обоснование выбора int для суммы (10^5 * 10^4 =
    10^9 при потолке 2.15 * 10^9, то есть запас всего вдвое), и почему
    максимум нельзя инициализировать нулём.
  * Тесты: обе охранные проверки, оба цикла, оба исхода Math.max, k = 1,
    k = n (второй цикл не начинается), все отрицательные числа, края
    диапазона значений, отдельный тест на дробное среднее 10.5 и предельная
    сумма 10^9 на массиве из 100 000 десяток тысяч.
  * ПОЙМАНА ОШИБКА В ТЕСТЕ (не в решении): для [4,0,4,0] с k=2 я записала
    ожидание 4.0, спутав максимальный элемент со средним. Правильно 2.0.
  * Визуализация: окно подсвечено рамкой, входящий элемент зелёным,
    выходящий розовым, три карточки состояния (windowSum, maxSum, среднее),
    причём среднее показывается только на последнем шаге — видно, что деление
    происходит один раз. Логика сверена с Java через Node на 12 кейсах.
- ИНИЦИАЛИЗАЦИЯ КАНДИДАТА ПЕРЕДЕЛАНА, вопрос Lela: почему majorityCandidate
  явно инициализируется нулём, ведь нуля в массиве может и не быть.
  Ответ технический: без инициализации не компилируется. Java требует
  definite assignment, а присваивание сидит внутри if внутри цикла, который
  формально может не начаться. Проверено запуском, фактический текст ошибки:
  «error: variable majorityCandidate might not have been initialized», два
  раза — на чтении в условии и на return.
  Но ноль и правда сбивает: он выглядит осмысленным, хотя затирается на
  первой же итерации. Перешли на классическую форму — majorityCandidate =
  nums[0], survivingVotes = 1, цикл с индекса 1. Инициализация перестала
  быть формальностью ради компилятора и стала первым шагом алгоритма.
- ГРАНИЦА МЕЖДУ ДВУМЯ КЛАССАМИ НАВЕДЕНА (требование Lela «мы сбились с пути,
  всё в кучу смешали»): MajorityElement теперь ровно по условию LeetCode,
  без проверок на пустой массив, тесты на пустой вход оттуда убраны.
  Проверка nums.length == 0 стоит первой строкой в MajorityElementNotGuaranteed,
  где гарантий нет. Итого 28/28 и 20/20 PASS, визуализация обновлена
  (первый шаг теперь показывает выдвижение nums[0] с одним голосом),
  логика сверена через Node на 15 кейсах.
  Синхронно обновлены .md (код, разбор, названия переменных в таблице прогона)
  и визуализация (панель кода, подписи карточек, тексты шагов). Логика
  визуализации пересверена с Java через Node на 16 кейсах.
- НА НАЙДЕННЫЙ БАГ НАПИСАН ТЕСТ, как просила Lela: инициализация nums[0]
  роняла метод на пустом массиве с ArrayIndexOutOfBoundsException, книжная
  majority = 0 доходит до конца цикла, который не начинается. Два теста:
  «не падает» и «возвращает начальное значение». По ограничениям задачи
  n >= 1, то есть вход внеусловный, — это зафиксировано в комментарии.
  Итого MajorityElement 30/30 PASS.

## 2026-08-22 — вторая задача блока 4, окно переменной ширины

- ПЕРЕИМЕНОВАНИЕ В MaxAverageSubarray сделала Lela: индекс второго цикла стал
  windowEnd, вычитаемый индекс вынесен в отдельную переменную windowStart.
  Имена назвали роли, и комментарий про «кто вошёл, кто вышел» стал не нужен.
  Правило записано в CLAUDE.md третьей добавкой к именованию: если переменную
  цикла МОЖНО назвать осмысленно — назвать; нейтральные currentIndex/readIndex
  годятся, только когда у индекса нет роли, кроме «идём по массиву».
- НОВАЯ ЗАДАЧА: LongestSubstringNoRepeat (LeetCode 3, Medium). 36/36 PASS.
  * src/block04_sliding_window/LongestSubstringNoRepeat.java — ДВА метода
    в одном классе, оба прогнаны на одних и тех же тестах:
    lengthOfLongestSubstring (состояние — HashSet символов окна, цикл while
    сжимает окно по одному символу) и lengthOfLongestSubstringWithJump
    (состояние — карта «символ → последняя позиция», левая граница прыгает
    сразу за прошлое вхождение).
  * docs/problems — условие, 4 примера, разбор порядка действий внутри
    итерации (сжать → добавить → измерить), две ловушки и амортизационный
    анализ. Ловушка первая: в варианте с прыжком Math.max при сдвиге
    windowStart обязателен, иначе на "abba" граница пятится назад к позиции
    символа ВНЕ окна и ответ выходит 3 вместо 2. Ловушка вторая, общая:
    сброс windowStart на текущую позицию ломается на "dvdf" (2 вместо 3).
  * Тесты: обе охранные проверки, пустая строка (цикл не начался), строка без
    повторов, повтор в начале, два шага сжатия за одну итерацию ("abcb"),
    все символы одинаковые, оба исхода Math.max, "abba" и "dvdf" на ловушки,
    пробелы и знаки, предельный вход 100 000 символов и строка из 512 различных
    значений char.
  * ПОЙМАНЫ ДВЕ ОШИБКИ В .md, обе нашла Lela по тексту LeetCode.
    Первая: в пояснении к примеру 3 стояло «между p и w пропущена буква» —
    неверно вдвойне, потому что в "pwke" не взята позиция 2 (второй w),
    а слова «пропуск» в условии нет вовсе, это была моя вольная расшифровка
    термина subsequence. Переписано через номера позиций.
    Вторая: ограничение длины стояло 5 * 10^4 вместо 10^5. Поправлено вместе
    с оценкой наивного решения (10^10 операций) и предельным тестом в классе.
  * РАЗДЕЛ «Подстрока и подпоследовательность» по требованию Lela: что это
    за термины и что называется пропуском, с ASCII-схемами. Схемы здесь —
    сознательное исключение из соглашения «иллюстрации отдельными .svg»:
    материал слушается вслух через Natural Reader, картинка в нём бесполезна.
    СНАЧАЛА Я ВЫНЕСЛА ЭТО ОТДЕЛЬНЫМ ФАЙЛОМ docs/SubstringVsSubsequence.md,
    и это оказалось лишней сущностью: Lela попросила убрать файл и вписать
    объяснение прямо в разбор задачи, между условием и примерами. Файл удалён.
  * ЧЕСТНАЯ ГРАНИЦА УТВЕРЖДЕНИЯ (правило про обещания шире ограничений):
    в .md сказано ровно то, что проверено, — 512 различных char работают,
    а за пределы BMP решение не заявляется, потому что эмодзи хранятся
    суррогатной ПАРОЙ char и такого теста нет.
  * Опора в книге сверена по PDF, а не по OCR: задача 1.1 «Уникальные
    символы», вопрос на с. 82, решение на с. 197. Оттуда взято рассуждение
    «строка длиннее алфавита обязана содержать повтор» — из него следует
    оценка памяти O(min(n, размер алфавита)).
  * Визуализация: два режима в одном файле (множество и карта позиций),
    подписи границ start/end под индексами, панель состояния с содержимым
    множества или карты. В режиме карты записи о символах вне окна показаны
    бледным пунктиром — видно, ради чего стоит Math.max. Логика сверена
    с Java через Node на 15 кейсах, плюс два инварианта проверяются на каждом
    шаге: множество совпадает с окном, в окне нет повторов.
- ШПАРГАЛКА ПОПОЛНЕНА: приём 8 «восстанови инвариант до того, как измеришь
  результат» — про порядок «починить → применить → измерить» внутри итерации.
  Второй пример к нему от обратного: у окна фиксированной ширины починка
  занимает одну строку, поэтому приём там не виден.
- Pattern_SlidingWindow.md освежён: пометки «класса пока нет» заменены
  ссылками на новый класс, в «См. также» добавлена вторая задача блока.
- ТАБЛИЦЫ СВОЙСТВ РАЗЪЕХАЛИСЬ ПО ВСЕМ 15 РАЗБОРАМ (просьба Lela). Раздел
  «Сложность» влит в новый раздел «Свойства решения» / «Свойства решений»:
  время и память стали первыми строками таблицы, к ним добавились остальные
  характеристики — число проходов, ранний выход, изменяет ли вход, требования
  к входу, работа на потоке, детерминированность, завершаемость, деградация.
  Набор строк подбирался ПО ЗАДАЧЕ, а не по общему шаблону — так решила Lela,
  чтобы таблицы не заполнялись водой.
  Что всплыло по ходу:
  * MergeSortedArray устойчив (stable), и это следствие строгого `>`
    в сравнении: при равных элементах в хвост уходит элемент из nums2;
  * у ValidAnagram проверка длин есть в эталоне и в варианте с сортировкой,
    но НЕТ в юникодном — там она не нужна, разное число символов само даёт
    ненулевой счётчик;
  * у MajorityElement со счётчиком частот ранний выход ВОЗМОЖЕН, но в коде
    его нет — записано именно так, чтобы таблица не обещала лишнего;
  * у GroupAnagrams порядок групп в ответе не гарантирован (обход HashMap),
    а порядок строк внутри группы сохраняется;
  * у задач со встречным движением работа на потоке невозможна в принципе —
    нужен доступ к обоим концам сразу.
  Соглашение записано в CLAUDE.md, в раздел про соглашения по коду.
- ВОПРОС LELA ПРО ИЗМЕРЕНИЕ ДЛИНЫ ОКНА: почему длина считается арифметикой
  по границам, а не размером структуры, и зачем в ней плюс единица.
  Ответ оказался разным для двух методов, и по её просьбе код переписан:
  * в варианте с МНОЖЕСТВОМ теперь стоит windowChars.size() — множество
    и окно совпадают по содержимому, это инвариант метода, поэтому размер
    множества и есть длина окна;
  * в варианте с ПРЫЖКОМ так делать нельзя: карта помнит и покинувшие окно
    символы. Контрпример "aabbccdd" — различных символов 4, а самое длинное
    окно "ab" длиной 2, то есть lastSeenIndex.size() вернул бы 4.
    Там осталась арифметика windowEnd - windowStart + 1.
  Плюс единица — потому что обе границы включены: без неё считалось бы
  расстояние между границами, а не число символов (проверка на окне
  из одного символа: разность 0, длина 1).
  Math.max нужен потому, что длина окна НЕ монотонна: после сжатия окно
  короче, и на "abcb" простое присваивание затёрло бы найденную тройку.
  Всё это выписано в .md отдельными разделами, 36/36 PASS после правки,
  панель кода в визуализации обновлена под новую строку.
- ИМЯ windowStart В MaxAverageSubarray ОКАЗАЛОСЬ ЛОЖНЫМ, поймала Lela
  вопросом «почему windowEnd - k, а не windowEnd - k - 1, ведь windowEnd
  только что увеличился». Разбор: инкремент срабатывает в конце итерации,
  поэтому внутри тела windowEnd — индекс ВХОДЯЩЕГО элемента, а windowEnd - k —
  индекс уходящего. Но само имя windowStart обещало левую границу окна,
  которая на самом деле находится на windowEnd - k + 1, то есть переменная
  указывала на позицию ВНЕ окна — начало предыдущего окна. Имя и провоцировало
  искать поправку на единицу.
  Переименовано в leavingIndex в классе, в .md и в обеих визуализациях блока.
  Заодно в MaxAverageSubarray.html панель кода приведена к именам класса
  (была на однобуквенном i), а переменные шагов переименованы следом.
  Тесты 18/18 PASS, логика визуализации пересверена через Node на 12 кейсах.
- MaxAverageSubarray.html ПРИЧЁСАН ПО ОБРАЗЦУ LongestSubstringNoRepeat.html:
  двухколоночная вёрстка, липкая панель шагов с полоской прогресса, легенда
  и пояснения к карточкам в правой колонке (видны во время шагов), готовые
  примеры уехали вниз, панель кода ограничена по высоте.
  Цвета согласованы между двумя визуализациями блока: входящий элемент теперь
  голубой (был зелёный), зелёный остался только за лучшим окном на финале.
  ЗАМЕЧАНИЕ LELA: перевёрстку второй визуализации надо было ПРЕДЛОЖИТЬ самой,
  когда правился первый файл. Правило «не лезть без просьбы» запрещает молча
  править, но предлагать оно не запрещает.
- ИНДЕКС ПЕРВОГО ЦИКЛА В MaxAverageSubarray ТОЖЕ ПЕРЕИМЕНОВАН: был
  currentIndex «за неимением роли», стал windowEnd. Роль всё-таки есть —
  на каждом шаге первого цикла окно занимает [0..windowEnd], то есть индекс
  двигает ту же правую границу, что и во втором цикле. Разными циклы делает
  не индекс, а тело: в первом сумма только прибавляется, во втором ещё
  и вычитается. Синхронно обновлены .md и панель кода в визуализации,
  18/18 PASS, логика визуализации пересверена на 12 кейсах.
- Pattern_SlidingWindow.md ДОГНАЛ ОСТАЛЬНОЙ БЛОК: в примере для окна
  фиксированной ширины стояло то же ложное windowStart, то есть обзорный файл
  учил имени, которое соседний разбор объявляет ошибкой. Заменено
  на leavingIndex, добавлено объяснение почему. В «частые ловушки» дописаны
  два пункта: мерить длину окна размером структуры, когда структура хранит
  лишнее, и ложное имя у вычисляемого индекса.
- ИНСАЙТ LELA ПРИ ПРОСЛУШИВАНИИ PDF: она несколько дней не могла понять,
  почему в MaxAverageSubarray два цикла, и думала, что дело только в подсчёте
  начального значения. А поняла так: циклы различаются ЦЕЛЬЮ — первый
  наращивает окно, второй двигает готовое. Её формулировка легла в новый
  раздел «Почему циклов два», туда же вариант со слиянием в один цикл,
  где фаза превращается в два if и искусственный Integer.MIN_VALUE.
- НОВОЕ СОГЛАШЕНИЕ ПО ПРОСЬБЕ LELA: в каждом разборе появляется раздел
  «Что делает каждая переменная» — таблица «переменная → что хранит → зачем»,
  по строке на КАЖДУЮ сущность решения, включая входные параметры
  и промежуточные структуры. Причина её словами: общая картина после разбора
  складывается, а на деталях начинаются провалы — непонятно, за что отвечает
  конкретный указатель или конкретная карта. Записано в CLAUDE.md,
  в MaxAverageSubarray.md сделано как образец.
  При этом Lela отдельно попросила следить, чтобы не появлялось трёх
  пересказов одного и того же: старые абзацы про имена индексов свёрнуты
  в строки таблицы, разбор ловушки с windowStart оставлен один раз.
- РАЗДЕЛ «Что делает каждая переменная» РАЗОШЁЛСЯ ПО ВСЕМ 15 РАЗБОРАМ.
  Для каждой задачи таблица собиралась ПО КОДУ КЛАССА, а не по памяти:
  сначала читался .java, потом заполнялись строки. Где в классе несколько
  методов, разобраны все — альтернативные решения получили либо свою таблицу
  (MajorityElement, LongestSubstringNoRepeat), либо абзац о том, чем их набор
  переменных отличается (ValidAnagram, MoveZeroes, TwoSum, ValidParentheses,
  FirstUniqueChar).
  Акцент по просьбе Lela — не «что делает решение», а «зачем заведена эта
  сущность и что сломается без неё». Поэтому в таблицах регулярно объясняется
  и то, чего в коде НЕТ: почему в ContainsDuplicate множество, а не карта;
  почему в TwoSum ключом стоит значение, а не индекс; почему в FirstUniqueChar
  не нужны ни флаг «нашли», ни переменная «лучший кандидат».
  ПОЙМАНЫ ПРИ СВЕРКЕ (тексты были неточны, поправлено):
  * в TwoSumSorted было сказано, что sum сравнивается с target трижды, —
    сравнений два, на равенство и на «меньше»;
  * там же обоснование сдвига указателя опиралось на фразу «справа элементов
    уже нет», хотя правильный аргумент другой: nums[rightIndex] — наибольший
    среди ОСТАВШИХСЯ кандидатов;
  * в LongestSubstringNoRepeat enteringChar был назван используемым трижды,
    фактически дважды.
- ПОРЯДОК РАЗДЕЛОВ В LongestSubstringNoRepeat.md ПЕРЕСТРОЕН, замечание Lela:
  таблица переменных для варианта с прыжком стояла сразу после таблицы для
  варианта с множеством — то есть ДО того, как сам прыжок был показан. Читать
  её было не по чему: второе решение в файле вообще не было описано, только
  фрагмент кода в разделе про два способа сдвига.
  Теперь так: идея и код первого решения -> его таблица переменных -> разборы,
  относящиеся к нему; затем отдельный раздел «Второе решение: прыжок левой
  границы» с идеей, ПОЛНЫМ кодом метода и своей таблицей переменных; затем
  следствия именно этого решения (почему размер карты не годится, зачем
  Math.max); в конце общий раздел «Общее для обоих решений».
  Правило на будущее: сначала показать решение, потом объяснять его переменные.
- КАРТА ПЕРЕИМЕНОВАНА: lastSeenIndex -> lastIndexByChar. Вопрос Lela был про
  перевод («последний попавшийся индекс?»), и на нём вскрылось, что имя
  описывает ОДНО число, тогда как переменная — карта: про ключ приходилось
  догадываться по типу Map<Character, Integer>. Переименовано в классе, .md
  и визуализации, 36/36 PASS, логика визуализации пересверена на 15 кейсах.
- ДОБАВЛЕН РАЗДЕЛ «Имена переменных и английский за ними» по просьбе Lela:
  таблица «имя -> дословно -> по-русски -> что стоит знать» по всем именам
  решения, разбор конструкции lastSeen… (lastSeenAt, lastSeenVersion), две
  схемы именования карт (значениеByКлюч и ключToЗначение) и почему нельзя
  называть переменную lastIndexOfChar — в Java это имя МЕТОДА у String
  и List. Отмечено, что в TwoSum используется вторая схема (seenValueToIndex),
  и это осознанная разница, а не разнобой.
- СОЗДАН docs/glossary.md по просьбе Lela. Повод: слово «инвариант» встречается
  ей не первый раз, а значение не держится в голове. Тринадцать терминов —
  инвариант, амортизированная сложность, амортизационный анализ, фолбэк,
  ранний выход, in-place, устойчивость, отпечаток, онлайн-алгоритм, кодовая
  точка и суррогатная пара, LIFO/FIFO, подстрока против подпоследовательности,
  definite assignment. У каждого: что значит, откуда слово, где встречается
  в проекте — все ссылки проверены на существование файлов.
  Приём, который сработал для «инварианта»: показать этимологию (in- + varias,
  «не варьирующийся») и сразу назвать частую путаницу — инвариант это не
  неизменная величина, величины как раз меняются, неизменной остаётся правда
  о них. Плюс предупреждение про омоним в дженериках.
  Файл вписан в CLAUDE.md рядом с heuristics.md: приёмы — там, термины — тут.
- СБОРЩИК PDF НАУЧЕН РАЗЛИЧАТЬ ДВЕ РОЛИ СТРЕЛКИ. Раньше ко всем «→» ставилась
  подсказка «значит», и запись карты «символ → последняя позиция» читалась
  вслух как «символ значит последняя позиция». Теперь в tools/tts_pdf.py есть
  функция arrow_word: стрелка внутри кавычек-ёлочек или внутри инлайнового
  кода считается ОТОБРАЖЕНИЕМ и озвучивается словом «соответствует», в
  остальных случаях это ВЫВОД и остаётся «значит». Плюс после подсказки
  ставится пробел, если следом идёт не пробел: в записи a→0 синтез слышал
  «соответствует0».
  МОЙ ПРОМАХ, на который указала Lela: я начала править сами .md, хотя
  правило проекта прямо говорит — исходники под озвучку не трогаем, все
  подгонки живут в сборщике. Две правки в LongestSubstringNoRepeat.md
  откачены. Прежде чем что-то менять, надо понять, где источник проблемы:
  цитата была скопирована из PDF, а не из markdown.
- Pattern_SlidingWindow.md ПЕРЕПИСАН, требование Lela: обзорный файл блока
  превратился в сборник дублей — код решений, ловушки, разбор имён переменных,
  всё то же самое, что в разборах задач. Оставлено только то, ради чего файл
  и нужен: что объединяет задачи блока, два подвида одной фразой, отличие
  от блока «два указателя» (у двух указателей — две независимые позиции
  с разными ролями, у окна — границы одного участка плюс его состояние),
  сигналы распознавания и вопрос про вложенный while. Выброшены разделы
  с кодом, «частые ловушки» и всё, что повторяло MaxAverageSubarray.md
  и LongestSubstringNoRepeat.md. Размер файла упал примерно вдвое.
  Правило записано в CLAUDE.md, там же признаки, что обзорный файл поехал
  не туда: появился код решения, ловушки с разбором, имена переменных
  или таблица по шагам.
- ПЕРЕД ЗАКРЫТИЕМ ЧАТА собраны в CLAUDE.md все замечания, которые за сессию
  прозвучали, но нигде не были записаны. Новые разделы:
  * «Предлагать, а не молчать» — правило про правки запрещает молча ПРАВИТЬ,
    но не запрещает ПРЕДЛОЖИТЬ; повод — перевёрстка второй визуализации,
    про которую я не заикнулась;
  * «Язык изложения» — без жаргона («прогон»), проверять, говорят ли так
    по-русски вообще («обновляемая НА разницу», «данные ПОД ним меняются»),
    метафора только рабочая, незнакомый термин идёт в глоссарий, числа в тексте
    («используется трижды») сверять с кодом;
  * «Порядок изложения в разборе» — сначала решение словами и кодом, потом
    таблица переменных; каждый метод класса должен быть ОПИСАН, а не просто
    показан;
  * «Не плодить сущности» — новое пояснение идёт разделом в существующий файл,
    отдельный файл только по просьбе (история с SubstringVsSubsequence.md);
  * в раздел Git — про разнесение разнотемных изменений по коммитам и про
    закомментированные тесты;
  * в раздел PDF — про то, что цитата может быть из PDF, а не из markdown,
    и тогда чинить надо сборщик; плюс таблица подсказок дополнена второй ролью
    стрелки.
  В journal/todo.md добавлена сводка сделанного за сессию — для контекста
  нового чата.

## 2026-08-30 — начат блок 5 «Бинарный поиск»

- НОВАЯ ЗАДАЧА, первая в блоке 5: BinarySearch (LeetCode 704). 23/23 PASS.
  * src/block05_binary_search/BinarySearch.java — цикл с включительными
    границами lowIndex/highIndex, середина через
    lowIndex + (highIndex - lowIndex) / 2, порядок веток «сначала совпадение,
    потом меньше/больше». Одно решение, без альтернатив: требований, которые
    закрывал бы второй метод, в условии 704 нет.
  * docs/problems/block05_binary_search/BinarySearch.md — условие, 4 примера,
    инвариант «если target есть, он на отрезке [lowIndex..highIndex]», таблица
    переменных, «Свойства решения», разбор по шагам на двух примерах (найдено
    и не найдено), таблица числа итераций от длины массива.
  * docs/visualizations/block05_binary_search/BinarySearch.html — участок
    поиска синим, середина голубой, отсечённое тускнеет; карточки состояния
    показывают «осталось ячеек» и число сравнений. Девять пресетов, среди них
    неотсортированный вход. Логика сверена с Java через Node на 17 кейсах
    плюс сплошная проверка на 1000 элементов — совпала.
- ОХРАННЫЕ ПРОВЕРКИ разведены по причинам: null отвергается исключением,
  а пустой массив пропускается сознательно — highIndex становится -1, цикл
  не начинается, метод возвращает -1. В комментарии это оговорено, чтобы
  отсутствие проверки читалось как решение, а не как недосмотр.
- ОТСОРТИРОВАННОСТЬ ВХОДА названа ПРЕДУСЛОВИЕМ, и это отдельный раздел
  в .md: проверить её стоило бы O(n) и съело бы весь выигрыш от O(log n).
  Опасность в том, что при нарушении метод не падает, а молча возвращает
  неверный ответ, — поэтому в визуализации есть пресет [5,1,9,3,12] с target 3,
  где тройка в массиве есть, а поиск даёт -1.
  Термин «предусловие» добавлен в docs/glossary.md, с отличием от охранной
  проверки: охранная проверка — то, что метод контролирует сам.
- heuristics.md: добавлен ПРИЁМ 9 «Каждая итерация обязана уменьшать задачу».
  Повод — единицы в сдвигах границ. Проверено обоими краями: без единицы
  зависает и левая граница (массив [1,2], target 2), и правая (массив [1],
  target 0). Первая формулировка была неверной — я написала, что «зависающий»
  случай бывает только у левой границы из-за округления середины влево;
  на односимвольном участке зависает и правая. Исправлено в .md.
  Туда же дописаны примеры к приёму 1 (строгое или нестрогое сравнение решается
  тем, включена ли граница в непроверенное) и к приёму 3 (фолбэк -1 после цикла).
- СВЕРКА С КНИГОЙ по оригиналу PDF, а не только по RAG: код бинарного поиска
  у Макдауэлл на с. 148–149, в двух видах — цикл и рекурсия. Два отличия
  от нашего кода выписаны таблицей в .md: книга считает середину как
  (low + high) / 2 (переполнение) и ставит совпадение последней веткой.
  Отдельной задачи-упражнения на простой бинарный поиск в книге нет.
- ЧЕСТНАЯ ОГОВОРКА В РАЗБОРЕ: на этой задаче переполнение полусуммы
  НЕВОЗМОЖНО — массив не длиннее 10^4. Разностная форма нужна как привычка
  для поиска по диапазону значений. Числа в разборе подтверждены тестами:
  2 000 000 000 + 2 100 000 000 в int даёт -194 967 296, середина -97 483 648.
- Числа в тексте сверены запуском: худшее число сравнений 10 на 1000 элементах
  и 5 на 16 элементах — оба значения получены в прогоне, а не выведены на бумаге.
