# Linux JDK tarball (для песочницы Claude, не нужен в репозитории)
*.tar.gz

# IntelliJ output
out/
target/

# Настройки IDE — у каждого свои, в репозитории не нужны
.idea/
*.iml
__pycache__/

# Копии файлов, отправленных в чат: их кладёт сюда десктопное приложение Claude
Claude outputs/
